<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>

  	<% include templates/header.ejs %>

  	<ul>
  		<% recipedata.results.forEach(function(item){ %>
  			<li> <%= item.title %> </li>
  		<% }); %>
  	</ul>
    
  </body>
</html>
